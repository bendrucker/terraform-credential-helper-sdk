on: [push, pull_request]
name: tests
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Install Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.14.x
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Test
      run: go test -v ./...